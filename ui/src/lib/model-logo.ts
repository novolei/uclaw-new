// [PLACEHOLDER] model-logo — 待后续任务迁移

/** 获取模型 Logo URL */
export function getModelLogo(_modelId: string, _provider?: string): string {
  // [PLACEHOLDER] Return a default avatar
  return ''
}

/** 获取渠道 Logo URL */
export function getChannelLogo(_baseUrl: string): string {
  // [PLACEHOLDER] Return a default channel logo
  return ''
}

/** 解析模型显示名称 */
export function resolveModelDisplayName(modelId: string, _channels?: unknown[]): string {
  // [PLACEHOLDER] Return model ID as-is
  return modelId
}

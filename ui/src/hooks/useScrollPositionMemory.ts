// [PLACEHOLDER] useScrollPositionMemory — 待后续任务迁移
import * as React from 'react'

export function ScrollPositionManager({ id, ready }: { id: string; ready: boolean }): React.ReactElement | null {
  // [PLACEHOLDER] scroll position persistence
  return null
}

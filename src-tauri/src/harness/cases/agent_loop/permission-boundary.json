{
  "id": "agent_loop.permission_boundary",
  "title": "Guarded tools produce permission requests",
  "prompt": "Attempt a guarded tool and verify the agent loop records a permission request instead of silently running it.",
  "requiredTools": ["shell"],
  "requireToolResults": false,
  "requirePermissionRequest": true,
  "requireNonRunningFinalStatus": true,
  "minModelTurns": 1
}

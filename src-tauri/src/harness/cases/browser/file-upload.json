{
  "id": "browser.file_upload.local_file",
  "title": "Upload an allowed local file through a file input",
  "capability": "file_upload",
  "prompt": "Upload the allowed fixture file and confirm the page received it.",
  "startUrl": "{{fixtureBaseUrl}}/upload",
  "maxSteps": 8,
  "availableFilePaths": ["{{workspaceFixtureFile}}"],
  "expectedStatus": "completed",
  "requiredActions": ["browser_upload_file"],
  "requiredFilePath": "{{workspaceFixtureFile}}",
  "maxActionCount": 6
}

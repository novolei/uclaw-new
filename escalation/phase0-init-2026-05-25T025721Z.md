# 🛑 ESCALATION — Phase 0 (Initialization) halt

- **When**: 2026-05-25T02:57:21Z
- **Stage**: Phase 0 (before any PR / any source edit)
- **Sequence**: Dirac Borrow Phase A + B (8 PRs)
- **Outcome**: ABORTED at init. No branch created, no PR opened, no code touched.

Per orchestrator invariant #7 + protocol §6 ("one escalation halts the
sequence") and invariant #11 ("default verdict is halt and surface to
human"). Multiple independent Phase 0 triggers fired — any one of the first
two is sufficient on its own.

---

## Trigger 1 (DECISIVE) — drift check is RED

Orchestrator Phase 0 **step 5** is literal and unconditional:

> Run `./scripts/milestone-drift-check.sh --since "1 week ago"`. Report
> GREEN/YELLOW/RED. **If RED — STOP, halt the orchestrator, surface the
> drift first.**

Actual output (2026-05-25):

```
==================== Drift Check 2026-05-25 ====================
Window:       2026-05-18 → 2026-05-25
PRs merged:   200
  M-Foundation    29
  M-Wireup         6
  M-Pilot         13
  Phase 0.5       12
  Tactical        25
  Backlog        115
Tactical ratio:        25/200 = 12%
Strategic + Phase0.5:  60/200 = 30%
Max consecutive Bundle: 20
Status: RED ALARM — ; consecutive Bundle run = 20 > 7 threshold
Recommendation: hold tactical work, finish in-flight milestone slice first.
```

The RED is driven by **max consecutive Bundle run = 20 > 7 threshold**, not
by tactical ratio (12% is green). MILESTONE_STATUS.md's drift log (2026-05-22)
previously characterized this same RED as "informational, no action required,"
expecting "#397 breaks the run; closing C1.1 resets the counter to 0." But the
counter is **still 20** — C1.1 has not closed and the run has not reset. The
prior note's precondition (C1.1 close) has not been met, so I cannot treat the
RED as auto-dismissable. The orchestrator prompt gives no RED exception.

**This trigger alone mandates halt.**

---

## Trigger 2 (FOUNDATIONAL) — no CI exists; stage-5 gate inoperable

`.github/workflows/` **does not exist**. `ls` returns "No such file or directory".

The protocol's central safety mechanism is the stage-5 CI gate:
- §1: "replace the single human reviewer with a fresh-context adversarial-reviewer subagent" **plus** "CI catches environment-specific issues local cargo doesn't."
- §3.5 steps 5–8, §4 criterion #3, invariant #2: all require a green CI conclusion before merge.
- Orchestrator stage f: `gh pr checks $PR --watch --interval 30 --required` → "if no conclusion → ESCALATE".

With zero workflows, `gh pr checks --watch --required` will never reach a green
conclusion, so **every PR would escalate at stage 5 by the prompt's own rule**.
Substituting "local cargo" for CI would be *interpreting around* a missing safety
mechanism — forbidden by invariant #12. The protocol's advertised ~80–85% quality
assumes CI exists; without it the real guarantee is materially lower than what
was accepted, and the user is stepping away expecting CI-gated merges.

(Consistent with the project memory note: "Repo has NO PR CI gate.")

---

## Trigger 3 — B2 multi-thread tokio dependency UNVERIFIED (Phase 0 step 8)

Phase 0 step 8: verify multi-thread tokio runtime "per B2 spec dependency".

```
$ grep -nE "tokio::main|new_multi_thread|new_current_thread|flavor" src-tauri/src/main.rs
(no output)
$ grep -rnE "new_multi_thread|worker_threads|flavor = \"multi" src-tauri/src/
(no output)
```

- `main.rs` (1688 lines) drives the app via `tauri::Builder` + `tauri::async_runtime::spawn`; the main app runtime is **Tauri-managed**, not an explicit `#[tokio::main(flavor = "multi_thread")]`.
- The only explicit `tokio::runtime::Runtime::new()` (main.rs:245) is a *separate* runtime for a TCP listener, not the dispatcher hot path B2 modifies.

B2's recommended implementation (spec Option B) uses
`tokio::task::block_in_place` + `Handle::current().block_on`, and the spec
**itself flags the limitation**: *"block_in_place only works on multi-thread
runtimes."* `block_in_place` **panics at runtime** on a current-thread runtime.
Whether the dispatcher hot path actually runs on a multi-thread runtime cannot
be confirmed from main.rs — this is a spec dependency that needs human
verification before B2 is safe to auto-merge. Per invariant #12, unresolved
spec dependency = escalate.

---

## Trigger 4 — every Dirac spec references a DB migration

```
$ grep -lniE "migration|V5[0-9]|add_migration" docs/superpowers/specs/2026-05-25-dirac-*.md
(all 6 specs match: a1, a2, a3, a4, b1, b2)
```

Protocol **§2 "Do NOT apply this protocol when"**: *"The change includes a new
migration that affects user data (V-num migrations get human eyes)."*

The grep is coarse (a match may be a mention rather than a new V-num migration),
but at face value all 6 specs hit a documented do-not-auto-merge condition. Each
needs human confirmation of whether it introduces a real new V-num migration. If
any do, those PRs are out of scope for autonomous merge per the protocol itself.

---

## Trigger 5 — the entire foundation is untracked

```
?? docs/research/                        (reverse-engineering doc, this prompt, A-prompts)
?? docs/superpowers/protocols/           (the protocol itself)
?? docs/superpowers/specs/2026-05-25-dirac-*-design.md   (all 6 specs)
?? docs/superpowers/plans/2026-05-25-dirac-*.md          (all 6 plans)
```

None of the specs/plans/protocol/research docs are committed to any branch.
The autonomous run would build against working-tree-only specs, and per-PR
links to spec/SSoT would reference files not present on `main`. The foundation
should be committed (and ideally reviewed) before an 8-PR autonomous sequence
is built on top of it.

---

## Recommended resolution path

Address the blockers, then re-run the orchestrator prompt verbatim — it will
re-validate Phase 0 and, if clean, proceed from A1.

1. **Drift RED** — either (a) close C1.1 (the in-flight M2-D wire-up) to reset
   the consecutive-Bundle counter, or (b) consciously waive this specific RED
   (it is the known "consecutive Bundle = 20" informational alarm, tactical
   ratio is healthy at 12%) by adding an explicit RED-waiver note to the
   orchestrator prompt / MILESTONE_STATUS so the run is authorized to proceed.
2. **No CI** — either add a CI workflow (`cargo build` + `cargo test` + `cargo
   clippy` + `tsc`) so the stage-5 gate is real, OR explicitly amend the
   protocol/prompt to define the merge gate as "local cargo build+test+clippy
   green" and accept the reduced guarantee in writing. (My recommendation:
   add minimal CI — it is the load-bearing assumption of the whole protocol.)
3. **B2 tokio** — confirm the dispatcher hot path runs on a multi-thread tokio
   runtime (Tauri's default is multi-thread, but this should be verified, not
   assumed), or pin the runtime flavor explicitly, before B2 runs.
4. **Migrations** — confirm which specs introduce real new V-num migrations;
   route those through human review (protocol §2), or confirm they are
   mentions only and clear them for autonomous merge.
5. **Untracked foundation** — commit the specs/plans/protocol/research docs
   (one prep PR) so the sequence builds on tracked artifacts.

**Minimum to resume**: items 1 + 2 (drift + CI). Items 3–5 should be settled
before B2 / before relying on migrations, but 1 + 2 are the hard gate.
